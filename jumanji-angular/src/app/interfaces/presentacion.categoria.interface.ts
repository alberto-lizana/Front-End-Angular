/**
 * @description
 * Representa la información utilizada para mostrar una categoría
 * en la sección de presentación.
 */
export interface PresentacionCategoria {
  titulo: string;
  caracteristicas: string[];
  descripcion: string;
  espiritu: string;
  imagen: string;
  ruta: string;
}
